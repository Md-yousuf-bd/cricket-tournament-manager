const Navbar = ( ) => {
    return (
       <header className="h-16 bg-white shadow flex items flex items-center justify-between px-6">
           <h2 className="text-xl font-semibold">Cricket Tournament Management</h2>
           <div>
              admin
           </div>

       </header> 
    );
};

export default Navbar